ruby '2.2.2'
source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '>= 4.2.1'

# Use postgresql as the database for Active Record
gem 'pg', '>= 0.18.1'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0.3'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 2.7.0'
# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
gem 'coffee-rails-source-maps', '~> 1.4.0'
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
gem 'therubyracer', platforms: :ruby

# Use jQuery as the JavaScript library
gem 'jquery-rails', '~> 4.0.1'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
# gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.2'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Bower integration.
gem 'bower-rails', '>= 0.9.2'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Heroku wants this.
gem 'rails_12factor'

# Use Unicorn as the app server
gem 'unicorn'

# Use Foreman to manage app processes.
gem 'foreman', '>= 0.78.0', require: false

# Authentication.
gem 'authpwn_rails', '>= 0.17.2'
gem 'omniauth-google-oauth2', '>= 0.2.6'
#gem 'faraday', '~> 0.8.0'

# CSS assets.
gem 'foundation-rails', '= 5.5.1.2'
gem 'font-awesome-rails', '>= 4.3.0.0'

# E-mails for production exceptions.
gem 'exception_notification', '>= 4.0.1'

# reCAPTCHA for signing up.
# TODO(pwnall): use the gem when it learns the v2 API
# gem 'recaptcha', require: 'recaptcha/rails'

# Resizable components in the IDE.
gem 'jquery-ui-rails', '>= 5.0.3'

# JSON assets.
gem 'tilt-jbuilder', '>= 0.6.1', require: 'tilt/jbuilder'

# Slideshows.
gem 'slick_rails', '>= 1.4.1'

# Pagination.
gem 'kaminari', '>= 0.16.1'

# Base32 URLs.
gem 'base32', '>= 0.3.2'

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  #gem 'byebug'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  #gem 'spring'
end

group :development do
  # Fancy error pages.
  gem 'better_errors'
  gem 'binding_of_caller', '>= 0.7.2'
end

group :test do
  gem 'binary_fixtures', '>= 0.1.3'
end
